//window.onload = function () {
  //var platform = window.navigator.platform;

  //var os;

  //if (platform.search(/mac/i) > -1) os = 'osx';
  //if (platform.search(/win/i) > -1) os = 'windows';
  //if (platform.search(/linux/i) > -1) os = 'linux';

  //if (os) {
    //var buttons = document.getElementsByClassName('dl-' + os);
    //for (var i = 0; i < buttons.length; i++) {
      //var button = buttons[i];
      //button.className = button.className + ' button-primary';
    //}
  //}
//}